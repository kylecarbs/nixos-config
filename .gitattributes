# Patch files intentionally preserve upstream context whitespace; otherwise the
# outer Git diff treats patch syntax as repository whitespace errors.
*.patch whitespace=-space-before-tab,-blank-at-eol
